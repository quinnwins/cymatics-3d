name: Apply Anamnesis Review Fixes

on:
  push:
    branches:
      - codex/anamnesis-song-memory

permissions:
  contents: write

jobs:
  patch:
    if: github.actor != 'github-actions[bot]' && contains(github.event.head_commit.message, '[review-fixes]')
    runs-on: ubuntu-latest
    timeout-minutes: 3

    steps:
      - uses: actions/checkout@v4
        with:
          ref: codex/anamnesis-song-memory
          fetch-depth: 0

      - name: Apply deterministic review hardening
        run: |
          python3 scripts/fix-anamnesis-review.py
          git diff --check

      - name: Commit source and remove one-use scaffolding
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          rm scripts/fix-anamnesis-review.py
          rm .github/workflows/apply-anamnesis-review-fixes.yml
          git add -A
          git commit -m "fix: harden Anamnesis playback, intervals, and relics"
          git push origin HEAD:codex/anamnesis-song-memory

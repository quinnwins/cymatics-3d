name: Validate SoundForm

on:
  pull_request:
  push:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: validate-${{ github.workflow }}-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

jobs:
  test-build-browser:
    runs-on: ubuntu-latest
    timeout-minutes: 15

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Run tests
        run: npm test -- --reporter=verbose

      - name: Build production bundle
        run: npm run build

      - name: Run Sonic Memory in headless Chrome
        run: node scripts/verify-sonic-memory.mjs

      - name: Upload deployable build and visual proof
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: soundform-dist-and-sonic-memory-qa
          path: |
            dist
            qa_screenshots/sonic-memory-smoke.png
          if-no-files-found: ignore
          retention-days: 7
